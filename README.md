## Goal: to create a simple drag-n-drop yaml workflow maker.

### Backend involves:

single binary `golang` for throughput optimization. It will be compiled en-cloud in github workflow. This will be activated by pushing the particular code to golang branch.

Frontend will use `astro`


